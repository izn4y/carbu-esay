import express, { Request, Response } from "express";
const router = express.Router();

/**
 * @AccesData
 */

router.post("/searc", (request: Request, response: Response) => {

});

export default router;